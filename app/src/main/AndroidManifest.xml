<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Alarmas exactas para los recordatorios.

        Sin esto Android agrupa los avisos para ahorrar bateria y con el movil dormido los
        suelta cada nueve o quince minutos: el aviso que dice «empieza en 10 min» llegaba
        cuando la clase ya habia empezado.

        Se usa USE_EXACT_ALARM y no SCHEDULE_EXACT_ALARM porque la segunda viene denegada de
        fabrica en Android 14 y obliga a mandar al usuario a los ajustes del sistema. La
        primera se concede al instalar, pero Play solo la admite en apps de alarma, reloj o
        calendario: si UniStack llega algun dia a Play Store, hay que revisar esta linea.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!--
        El táctil de los botones. Normalmente vibra el sistema por cuenta de la app y no hace
        falta declararlo, pero MIUI/HyperOS reemplaza esa ruta por una que llama a `Vibrator`
        dentro del proceso de la app: sin el permiso, tocar cualquier botón con háptico lanzaba
        SecurityException y cerraba la app. Reportado el 21 ago 2026 en un Xiaomi con Android 16.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        Grabar un audio dentro de una nota, y nada mas.

        Se pide en el momento de tocar el microfono, no al instalar: quien nunca grabe nada
        no tiene por que conceder nada. La camara no aparece aqui a proposito: las fotos se
        hacen abriendo la camara del sistema, que devuelve el archivo ya hecho, y declarar
        CAMERA obligaria ademas a pedir ese permiso para algo que no lo necesita.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Sin esto, Android 11 en adelante no deja ni ver si hay una app que entienda «tg://», así
        que abrir el grupo de soporte fallaba antes de intentarlo. Solo declara lo que la app
        necesita abrir: Telegram y una página web.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".UniStackApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="UniStack"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniStack"
        android:enableOnBackInvokedCallback="false">

        <!-- WorkManager se inicializa bajo demanda desde UniStackApplication, no con este
             proveedor: ver el comentario de esa clase. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <receiver
            android:name=".core.notifications.ReminderReceiver"
            android:exported="false" />
        <!-- Los botones «Asistí» y «Falta» del aviso: nadie de fuera los dispara. -->
        <receiver
            android:name=".core.notifications.AttendanceActionReceiver"
            android:exported="false" />
        <receiver
            android:name=".core.notifications.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
